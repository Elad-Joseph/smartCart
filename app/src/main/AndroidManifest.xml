<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartCart.Light"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode"/>

        <activity
            android:name=".Activities.loginModel"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
<!--        <activity android:name=".loginModel"/>-->

        <activity android:name=".Activities.signInModel"/>

        <activity android:name=".Activities.HomePageModel" android:supportsRtl="true"/>
        <activity android:name=".Activities.ProfilePageModel" android:supportsRtl="true"/>
        <activity android:name=".Activities.BarcodeScannerModel" android:supportsRtl="true"/>
        <activity android:name=".Activities.ListDisplayModel" android:supportsRtl="true"/>
        <activity android:name=".Activities.FriendsListModel" android:supportsRtl="true"/>

    </application>
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-feature android:name="android.hardware.camera" android:required="true"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>


</manifest>